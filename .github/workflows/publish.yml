name: Auction Workflow (no scraper)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Scraper runs locally. Upload JamesEdition_feed_105029.xml to repo."
